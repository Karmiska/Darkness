#include "engine/SceneV2.h"

namespace engine
{
    namespace experimental
    {
        struct Transform
        {
            float x, y, z;
        };

        void Scene::test()
        {
            /*auto entity = m_ecs.create();
            m_ecs.attachToEntity(entity, Transform{});
            m_ecs.erase(entity);*/
        }
    }
}
