#pragma once

namespace engine
{
	enum class JitterOption
	{
		Disabled,
		Enabled
	};

	enum class AlphaClipOption
	{
		Disabled,
		Enabled
	};

	enum class DepthTestOption
	{
		GreaterEqual,
		Equal
	};

	enum class BiasOption
	{
		Disabled,
		Enabled
	};

    enum class DebugOption
    {
        Disabled,
        Enabled
    };
}
